# LogLab v1.9.4 更新说明

> 自 v1.8.3 以来的首个正式版。累积了 **UI 全面重写**与多项重要修复。

---

## 🎨 界面全面重写（V4 设计）

从 1.8.3 到 1.9.x，整个界面按新的 V4 设计语言重做：

- **全新配色与排版**：统一的圆角卡片、状态色体系、更紧凑的信息密度
- **快捷操作行**：按使用频率重排，最常用的动作放最左
  - 抓取页：*抓启动 · 选进程 · 搜索 · 筛选*
  - 实时页：*暂停 · 停止 · 选进程 · 搜索 · 关键词*
  - 崩溃页：*读历史 · 菜单 · ⋮*
- **底部 FAB**：抓取/监听主按钮统一为「开始 / Start」，与顶部开关不再混淆
- **日志列表瘦身**：每行不再重复打印时间戳（原本吃掉近半屏宽），改为 `[级别] Tag: 内容`；完整时间戳保留在详情页与导出文件中
- **深色模式修复**：此前"系统浅色 + App 深色"组合下标题和分组标题会变成深色叠深色（看不见），现已彻底修复
- **应用图标正常显示**：崩溃页此前图标不渲染，现改用 `loadIcon()` 并修正缓存键

## 🔧 崩溃记录修复

- **"抓到了但列表里不显示"**：`logcat -b crash` 在崩溃写入后会长时间静默，解析器一直等"下一行"导致事件卡在缓冲区。现在能识别块边界并立即发布
- **后台记录不刷新**：崩溃监控跑在前台服务，App 切回时列表不重组。新增 `RefreshOnResume`（仅 `ON_RESUME` 触发，不轮询）
- **清空无反应**：列表键依赖的刷新锚点在 `clear()` 时未推进，已修复
- **新增「分享诊断」**（崩溃页 ⋮ 菜单）：崩溃记录 + 运行日志 + 设备信息一键导出

## 📡 无线调试端口发现重做（本次重点）

### 问题

关闭无线调试 → 退出 App → 重新开启后，**App 扫不到新端口**。

日志显示系统 mDNS 已失效：

```
mDNS 第 1 轮：发现 40357   → 握手成功但命令执行失败（adbd 半死）
mDNS 第 2 轮：发现 35939   → 同样半死
```

系统 mDNS 解析器固守关闭前的旧记录，**反复回收那几个早已失效的端口**，而真正的新端口从未出现在任何通告里——等 60 秒以上也无济于事。

### 修复

新增**主动端口探测**作为兜底：

1. 先扫已知端口邻域（±3000）
2. 再并发扫全范围 `30000–65000`（回环连接约 1ms，实测 **1.6 秒**完成）
3. 结果限量 8 个候选，逐个用 `echo` 复验确认身份

### 效果

```
10:00:09 [CHECK] mDNS 未能提供可用端口，转入主动端口探测
10:00:10 [CHECK] 端口探测完成：扫描 30000-65000，命中 4 个候选（1586ms）
10:00:10 [CHECK] 主动探测端口 44245 验证通过，已自动切换
10:00:10 [UI]   检测到无线调试端口变化（45311 → 44245），已自动更新并连接
```

**全自动完成，无需人工查端口。**

### 附带改进

- **退出时清理 mDNS 缓存关联**：`ON_STOP` 时停止发现（令系统解析器丢弃该会话的缓存条目）+ 探活已知端口，失效的记入 30 分钟观察名单，下次启动不优先采用
- **「去开启」直达开发者选项**：此前误跳配对页，现正确区分"未开启 / 未配对 / 连不上"三种状态

---

## 📥 安装

| 项目 | 说明 |
|------|------|
| 文件 | `LogLab_v1.9.4.apk` |
| 架构 | arm64-v8a |
| 系统 | Android 8.0+ (minSdk 26) |
| 升级 | **v1.8.x / v1.9.x 可直接覆盖安装**（签名未变） |

---

## 📋 完整变更

详见 [CHANGELOG.md](CHANGELOG.md) 的 1.9.0 ~ 1.9.4 段落。
