# LogLab v1.9.6

**Release date:** 2026-09-21
**versionCode:** 30

---

## ⚠️ 关于这一版

开发过程中我曾尝试重构连接逻辑（拆分 `StartupCheck`、改变端口探测方式），
但**引入回归导致连接流程失效**。**这些改动已全部回退** —— 本版的连接逻辑
与 v1.9.5 **完全一致**（`core/` 目录逐字节相同）。

本版只保留了与连接无关的安全改进。

---

## 这次更新带来了什么

### 🆕 自动开启无线调试

配对过一次之后，LogLab 可以**自己打开「无线调试」** —— 重启手机、系统自动关掉开关之后，都不再需要手动去开发者选项里翻。

实现方式：连接成功后，LogLab 通过自己的 ADB 通道执行

```
pm grant com.loglab.app android.permission.WRITE_SECURE_SETTINGS
```

ADB shell 本来就有这个能力 —— 这正是 [Shizuku](https://github.com/RikkaApps/Shizuku) 获取该权限的方式。**不需要 root，不需要装 Shizuku，不需要电脑。**

拿到权限后写入三个系统设置：

| 设置项 | 作用 |
|--------|------|
| `adb_wifi_enabled=1` | 打开「无线调试」 |
| `ADB_ENABLED=1` | 打开「USB 调试」 |
| `adb_allowed_connection_time=0` | **连接永不过期**（默认 7 天） |

> ⚠️ **首次配对仍需手动。** 系统给的 6 位配对码只显示在开发者选项界面，App 读不到 —— 这是 Android 的设计，无法绕过。但**只需要一次**。

---

### ⏱ 自动开启后的等待窗口 6s → 15s

真机实测（一加 / Android 16）：

```
00:47:12  写入 adb_wifi_enabled=1
00:47:20  mDNS 才能看到新端口        ← 耗时 8 秒
```

原来的 6 秒窗口不够，白白浪费一轮。现在检测到"是我们自己开的无线调试"就用 15 秒窗口。

---

### 📦 体积优化：APK 小了约 1.2 MB

发现 `bcprov` 带着后量子密码（Picnic / SPHINCS）的查找表，是 `.properties` 资源文件，
`shrinkResources` 清不掉 —— 占了 APK 的 **22%**。LogLab 只用 RSA 做 ADB 认证，已排除。

另外：
- 引导图降到 480px 重新编码（省 62 KB）
- debug 构建也开 R8（之前 24.4 MB → 现在 9 MB）

**5.24 MB → 4.00 MB（-24%）**

---

## 🔧 内部改进（不影响功能）

- 两个前台服务的通知构建代码**逐字重复**，抽成公共实现（省 110 行）
- 清理了 39 处内联的全限定类名

---

## 🛠 新增加的工具

### `scripts/prepare-build.sh`

构建前自动回收内存。**这是为了解决 release 构建不稳定**：

沙箱可用内存有时只有 2.4 GB，而 Gradle + Kotlin + R8 需要 4~6 GB，
导致 R8 阶段 `OutOfMemoryError` 或构建从 5 分钟劣化到 20 分钟。

最常见的元凶是**上次构建残留的守护进程**（占 1~1.5 GB）。清理后实测可用内存
从 2.46 GB 回升到 4.02 GB。

排查经验见 `docs/BUILD_TROUBLESHOOTING.md`。

---

## 📥 安装

覆盖安装即可（签名不变，老用户可升级）。

```
LogLab_v1.9.6.apk
```

---

## 已知限制

- **首次配对必须手动**（系统限制，无法绕过）
- 自动开启无线调试依赖 `WRITE_SECURE_SETTINGS`，**部分厂商 ROM 可能拒绝写入** —— 失败时会降级为提示手动开启，不影响抓日志

---

## 反馈

遇到问题请附上 App 内「运行日志」导出内容，日志会记录每个阶段的判定依据。

