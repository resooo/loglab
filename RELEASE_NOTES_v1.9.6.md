# LogLab v1.9.6

## 🎉 重大更新：不再需要手动开启无线调试

以前每次手机重启、或者系统自动关掉无线调试之后，你都得去「设置 → 开发者选项 → 无线调试」手动打开一遍。

**现在 LogLab 能自己开了。**

配对过一次之后：

- 打开 App，它自己就把无线调试打开
- 手机重启后也一样
- 你甚至可以把无线调试关掉 —— 下次用的时候 LogLab 会自动开

### 怎么开启这个能力

**只需要做一次**：

1. 打开 LogLab → 设置页
2. 找到「自动开启无线调试」
3. 点一下「授权并开启」

之后就不用管了。这个授权是永久有效的，除非你卸载重装。

> 💡 **原理**：LogLab 连上无线调试后，会执行一条 ADB 命令给自己授权。这是 ADB 本身的标准能力（和 [Shizuku](https://github.com/RikkaApps/Shizuku) 用的是同一个方法），**不需要 root，不需要电脑**。

### 顺带解决的另一个烦恼

无线调试默认连接 **7 天就失效**，然后你得重新配对。现在 LogLab 会把它设成**永不过期**。

---

## 其他改进

### 🚀 切换页面不再卡顿

以前每次切页面、切后台回来，LogLab 都会重新检测一遍连接 —— 如果无线调试正好是关的，这个检测要跑 40 秒以上，界面就一直在转圈。

现在只要**刚检查过（30 秒内）且连接还好着**，就直接跳过，界面秒开。

### 📦 安装包小了 0.7 MB

5.22 MB → **4.50 MB**

清掉了一些用不到的密码算法表，图片也压缩了。功能没有任何减少。

---

## 已知限制

**首次配对仍然需要手动做一次。**

配对码只显示在系统的无线调试界面里，App 读不到 —— 这是 Android 的限制，绕不过去。但**只需要一次**，之后就不用再碰了。

少数厂商的 ROM 可能会拒绝 LogLab 写入无线调试开关。遇到这种情况 App 会明确告诉你，不会假装成功。

---

## 安装

下载下面的 `LogLab_v1.9.6.apk`，覆盖安装即可（签名没变，不会丢数据）。
