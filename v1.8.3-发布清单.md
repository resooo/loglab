# LogLab v1.8.3 发布清单（本机执行）

> 沙箱无法直连 GitHub，以下 3 步在你本机完成。

## 1. 合并沙箱提交到 GitHub

解压 `LogLab-repo-v1.8.3.zip`（完整 git 仓库，含 .git 与全部提交），然后：

```bash
cd 解压目录
git push origin main --tags
```

这次推送包含 1 个新提交（`774eb53` v1.8.3 主提交，基于 v1.8.2 之上）和 tag `v1.8.3`。

## 2. CI 自动发布

- push tag `v1.8.3` 后，GitHub Actions 自动构建 release APK 并挂到同名 Release
- Release 标题 = tag 名 = versionName（1.8.3）→ 应用内「检查更新」比较逻辑正常

## 3. 验证（真机装 APK，versionCode 22 / 1.8.2 可直接覆盖安装）

**核心场景（本次修复的误判）：**
1. **关闭无线调试后重启 App**：状态栏应显示红字「无线调试未开启，去开启」+「去开启」按钮（直达开发者选项）——**不再**显示「发现 N 个无线调试服务但连接不上」
2. **英文模式重复上面**：显示 "Wireless debugging is off — tap to enable"
3. **关闭无线调试后点开始抓取**：状态行显示「抓取失败：连接被拒绝——无线调试可能已关闭，开启后重试」（不再是一屏 ECONNREFUSED 异常文本）
4. **开启无线调试后重启 App**：正常自动连接（含端口变化自动切换）——确认修复没有误伤正常路径
5. **覆盖安装**：确认旧数据保留（配对状态、设置、崩溃记录）

> 场景 1 复现提示：关掉无线调试后**立刻**重启 App 最容易命中旧误判（adbd 半死 + mDNS 幽灵通告窗口期）。

## 本版改动速览

| # | 改动 | 说明 |
|---|---|---|
| ① | 验证结论分级 | 候选验证区分 OK / 半死（握手成功但 echo 失败）/ 不可达，直连、loopback 迁移、两轮候选全部覆盖 |
| ② | 半死信号定论 | 出现任意「adbd 半死」信号 → 直接判定无线调试未开启（mDNS 幽灵通告不再带偏结论） |
| ③ | 抓取友好报错 | ECONNREFUSED → 「无线调试可能已关闭，开启后重试」（中英） |
| ④ | 版本 | 1.8.3 / versionCode 22 |

发布说明文案可直接用仓库根 `CHANGELOG.md` 的 [1.8.3] 段落。
